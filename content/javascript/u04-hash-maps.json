[
  {
    "id": "js.u04.map.basics",
    "track": "javascript",
    "group": "Hash maps",
    "concept": "Map set and read back",
    "cue": "Insert two keys in order, then read the whole Map back.",
    "syntax": "map.set(key, val)",
    "kind": "predict",
    "prompt": "What does this evaluate to?",
    "snippet": "const m = new Map();\nm.set('a', 1);\nm.set('b', 2);\nm;",
    "expected": "Map(2) {\"a\" => 1, \"b\" => 2}",
    "prereqs": []
  },
  {
    "id": "js.u04.map.get-missing",
    "track": "javascript",
    "group": "Hash maps",
    "concept": "absent key reads undefined",
    "cue": "Ask what a Map returns for a key that was never set.",
    "syntax": "map.get(key)",
    "kind": "predict",
    "prompt": "What does this evaluate to?",
    "snippet": "const m = new Map([['a', 1]]);\nm.get('z');",
    "expected": "undefined",
    "prereqs": ["js.u04.map.basics"]
  },
  {
    "id": "js.u04.obj.stringify-keys",
    "track": "javascript",
    "group": "Hash maps",
    "concept": "object keys stringify",
    "cue": "Trace how a plain object coerces both a number and a string key.",
    "syntax": "obj[key] = val",
    "kind": "predict",
    "prompt": "What does this evaluate to?",
    "snippet": "const o = {};\no[1] = 'x';\no['1'] = 'y';\no[1];",
    "expected": "y",
    "prereqs": []
  },
  {
    "id": "js.u04.map.numeric-key",
    "track": "javascript",
    "group": "Hash maps",
    "concept": "Map keeps key type",
    "cue": "Compare the key type you stored against the key type you query.",
    "syntax": "map.has(key)",
    "kind": "predict",
    "prompt": "What does this evaluate to?",
    "snippet": "const m = new Map();\nm.set(1, 'a');\nm.has('1');",
    "expected": "false",
    "prereqs": ["js.u04.obj.stringify-keys"]
  },
  {
    "id": "js.u04.build.hash",
    "track": "javascript",
    "group": "Hash maps",
    "concept": "polynomial string hash mod size",
    "cue": "Accumulate a running hash across the characters left to right.",
    "syntax": "s.charCodeAt(i)",
    "kind": "write",
    "prompt": "Write `hashStr(s, mod)` that folds the string `s` into an integer in the range `[0, mod)` using `h = h * 31 + charCode` accumulated left to right, taking `% mod`.",
    "starter": "function hashStr(s, mod) {\n  // your code here\n}",
    "solution": "function hashStr(s, mod) {\n  let h = 0;\n  for (let i = 0; i < s.length; i++) h = (h * 31 + s.charCodeAt(i)) % mod;\n  return h;\n}",
    "tests": "for (const s of ['tea', 'eat', 'hello', '', 'a']) {\n  const h = hashStr(s, 8);\n  if (!Number.isInteger(h) || h < 0 || h >= 8) throw new Error('out of range: ' + s + ' -> ' + h);\n}\nif (hashStr('', 8) !== 0) throw new Error('empty must be 0');\nif (hashStr('tea', 8) !== hashStr('tea', 8)) throw new Error('must be deterministic');",
    "note": "Each character has a numeric code (charCodeAt); folding with * 31 lets position matter, so 'tea' and 'eat' land in different buckets.",
    "prereqs": []
  },
  {
    "id": "js.u04.class.this",
    "track": "javascript",
    "group": "Hash maps",
    "concept": "method reads and writes this, returns this to chain",
    "cue": "A method sees the instance through this; returning this lets the next call continue on the same object.",
    "syntax": "return this",
    "kind": "predict",
    "prompt": "What does this evaluate to?",
    "snippet": "class Box {\n  constructor() { this.items = []; }\n  add(x) { this.items.push(x); return this; }\n}\nconst b = new Box();\nb.add(1).add(2);\nb.items;",
    "expected": "[1, 2]",
    "note": "this is the receiver of the call; returning it enables method chaining like b.add(1).add(2).",
    "prereqs": []
  },
  {
    "id": "js.u04.build.set",
    "track": "javascript",
    "group": "Hash maps",
    "concept": "insert-or-update via chaining",
    "cue": "Scan the target bucket for a matching key before appending a new pair.",
    "syntax": "arr.push(item)",
    "kind": "write",
    "prompt": "Complete `set(key, val)` on `MyMap`: if `key` is already present, update its value; otherwise add it. Return `this`.",
    "starter": "class MyMap {\n  constructor(size = 8) {\n    this.n = size;\n    this.buckets = Array.from({ length: size }, () => []);\n  }\n  _hash(key) {\n    let h = 0;\n    const s = String(key);\n    for (let i = 0; i < s.length; i++) h = (h * 31 + s.charCodeAt(i)) % this.n;\n    return h;\n  }\n  set(key, val) {\n    // your code here\n    return this;\n  }\n}",
    "solution": "class MyMap {\n  constructor(size = 8) {\n    this.n = size;\n    this.buckets = Array.from({ length: size }, () => []);\n  }\n  _hash(key) {\n    let h = 0;\n    const s = String(key);\n    for (let i = 0; i < s.length; i++) h = (h * 31 + s.charCodeAt(i)) % this.n;\n    return h;\n  }\n  set(key, val) {\n    const b = this.buckets[this._hash(key)];\n    for (let i = 0; i < b.length; i++) {\n      const pair = b[i];\n      if (pair[0] === key) { pair[1] = val; return this; }\n    }\n    b.push([key, val]);\n    return this;\n  }\n}",
    "tests": "const m = new MyMap();\nm.set('a', 1);\nconst b = m.buckets[m._hash('a')];\nif (b.length !== 1 || b[0][0] !== 'a' || b[0][1] !== 1) throw new Error('should store one pair');\nm.set('a', 9);\nif (b.length !== 1 || b[0][1] !== 9) throw new Error('should update in place, not duplicate');",
    "prereqs": ["js.u04.build.hash", "js.u04.class.this"]
  },
  {
    "id": "js.u04.build.get",
    "track": "javascript",
    "group": "Hash maps",
    "concept": "lookup compares full keys",
    "cue": "Walk the key's bucket comparing each stored key.",
    "syntax": "for (let i = 0; i < arr.length; i++)",
    "kind": "write",
    "prompt": "Complete `get(key)` on `MyMap`: return the value stored for `key`, or `undefined` if absent.",
    "starter": "class MyMap {\n  constructor(size = 8) {\n    this.n = size;\n    this.buckets = Array.from({ length: size }, () => []);\n  }\n  _hash(key) {\n    let h = 0;\n    const s = String(key);\n    for (let i = 0; i < s.length; i++) h = (h * 31 + s.charCodeAt(i)) % this.n;\n    return h;\n  }\n  set(key, val) {\n    const b = this.buckets[this._hash(key)];\n    for (let i = 0; i < b.length; i++) {\n      const pair = b[i];\n      if (pair[0] === key) { pair[1] = val; return this; }\n    }\n    b.push([key, val]);\n    return this;\n  }\n  get(key) {\n    // your code here\n  }\n}",
    "solution": "class MyMap {\n  constructor(size = 8) {\n    this.n = size;\n    this.buckets = Array.from({ length: size }, () => []);\n  }\n  _hash(key) {\n    let h = 0;\n    const s = String(key);\n    for (let i = 0; i < s.length; i++) h = (h * 31 + s.charCodeAt(i)) % this.n;\n    return h;\n  }\n  set(key, val) {\n    const b = this.buckets[this._hash(key)];\n    for (let i = 0; i < b.length; i++) {\n      const pair = b[i];\n      if (pair[0] === key) { pair[1] = val; return this; }\n    }\n    b.push([key, val]);\n    return this;\n  }\n  get(key) {\n    const b = this.buckets[this._hash(key)];\n    for (let i = 0; i < b.length; i++) {\n      const pair = b[i];\n      if (pair[0] === key) return pair[1];\n    }\n    return undefined;\n  }\n}",
    "tests": "const m = new MyMap();\nm.set('tea', 1);\nm.set('eat', 2);\nif (m.get('tea') !== 1) throw new Error('tea');\nif (m.get('eat') !== 2) throw new Error('eat');\nm.set('tea', 5);\nif (m.get('tea') !== 5) throw new Error('updated value');\nif (m.get('nope') !== undefined) throw new Error('absent -> undefined');",
    "prereqs": ["js.u04.build.set"]
  },
  {
    "id": "js.u04.build.has",
    "track": "javascript",
    "group": "Hash maps",
    "concept": "membership without payload",
    "cue": "Reuse the bucket scan but report a boolean instead of the value.",
    "syntax": "=== strict equality",
    "kind": "write",
    "prompt": "Complete `has(key)` on `MyMap`: return `true` if `key` is present, else `false`.",
    "starter": "class MyMap {\n  constructor(size = 8) {\n    this.n = size;\n    this.buckets = Array.from({ length: size }, () => []);\n  }\n  _hash(key) {\n    let h = 0;\n    const s = String(key);\n    for (let i = 0; i < s.length; i++) h = (h * 31 + s.charCodeAt(i)) % this.n;\n    return h;\n  }\n  set(key, val) {\n    const b = this.buckets[this._hash(key)];\n    for (let i = 0; i < b.length; i++) {\n      const pair = b[i];\n      if (pair[0] === key) { pair[1] = val; return this; }\n    }\n    b.push([key, val]);\n    return this;\n  }\n  get(key) {\n    const b = this.buckets[this._hash(key)];\n    for (let i = 0; i < b.length; i++) {\n      const pair = b[i];\n      if (pair[0] === key) return pair[1];\n    }\n    return undefined;\n  }\n  has(key) {\n    // your code here\n  }\n}",
    "solution": "class MyMap {\n  constructor(size = 8) {\n    this.n = size;\n    this.buckets = Array.from({ length: size }, () => []);\n  }\n  _hash(key) {\n    let h = 0;\n    const s = String(key);\n    for (let i = 0; i < s.length; i++) h = (h * 31 + s.charCodeAt(i)) % this.n;\n    return h;\n  }\n  set(key, val) {\n    const b = this.buckets[this._hash(key)];\n    for (let i = 0; i < b.length; i++) {\n      const pair = b[i];\n      if (pair[0] === key) { pair[1] = val; return this; }\n    }\n    b.push([key, val]);\n    return this;\n  }\n  get(key) {\n    const b = this.buckets[this._hash(key)];\n    for (let i = 0; i < b.length; i++) {\n      const pair = b[i];\n      if (pair[0] === key) return pair[1];\n    }\n    return undefined;\n  }\n  has(key) {\n    const b = this.buckets[this._hash(key)];\n    for (let i = 0; i < b.length; i++) {\n      const pair = b[i];\n      if (pair[0] === key) return true;\n    }\n    return false;\n  }\n}",
    "tests": "const m = new MyMap();\nm.set('x', 0);\nif (m.has('x') !== true) throw new Error('present even with value 0');\nif (m.has('y') !== false) throw new Error('absent -> false');",
    "prereqs": ["js.u04.build.get"]
  },
  {
    "id": "js.u04.freq.map",
    "track": "javascript",
    "group": "Hash maps",
    "concept": "frequency count with Map",
    "cue": "Walk the string and track each character's running count in the Map.",
    "syntax": "(map.get(ch) || 0) + 1",
    "kind": "predict",
    "prompt": "What does this evaluate to?",
    "snippet": "const s = 'aabbbc';\nconst m = new Map();\nfor (let i = 0; i < s.length; i++) m.set(s[i], (m.get(s[i]) || 0) + 1);\nm.get('b');",
    "expected": "3",
    "prereqs": ["js.u04.map.basics"]
  },
  {
    "id": "js.u04.freq.map-full",
    "track": "javascript",
    "group": "Hash maps",
    "concept": "Map preserves insertion order",
    "cue": "Count every character, then read the whole Map in the order keys arrived.",
    "syntax": "for (let i = 0; i < s.length; i++)",
    "kind": "predict",
    "prompt": "What does this evaluate to?",
    "snippet": "const s = 'aabbbc';\nconst m = new Map();\nfor (let i = 0; i < s.length; i++) m.set(s[i], (m.get(s[i]) || 0) + 1);\nm;",
    "expected": "Map(3) {\"a\" => 2, \"b\" => 3, \"c\" => 1}",
    "prereqs": ["js.u04.freq.map"]
  },
  {
    "id": "js.u04.freq.zero-clobber",
    "track": "javascript",
    "group": "Hash maps",
    "concept": "|| treats stored 0 as falsy",
    "cue": "Note how a stored count of zero behaves under a falsy fallback.",
    "syntax": "value || fallback",
    "kind": "predict",
    "prompt": "What does this evaluate to?",
    "snippet": "const m = new Map();\nm.set('x', 0);\n(m.get('x') || 5);",
    "expected": "5",
    "note": "|| falls through on any falsy value, so a real stored 0 gets clobbered; ?? only falls through on null/undefined.",
    "prereqs": ["js.u04.freq.map"]
  },
  {
    "id": "js.u04.obj.count",
    "track": "javascript",
    "group": "Hash maps",
    "concept": "frequency count with Object",
    "cue": "Tally characters into a plain object as you scan.",
    "syntax": "obj[key] = (obj[key] || 0) + 1",
    "kind": "predict",
    "prompt": "What does this evaluate to?",
    "snippet": "const s = 'abca';\nconst freq = {};\nfor (let i = 0; i < s.length; i++) freq[s[i]] = (freq[s[i]] || 0) + 1;\nfreq;",
    "expected": "{a: 2, b: 1, c: 1}",
    "prereqs": ["js.u04.obj.stringify-keys", "js.u04.freq.map"]
  },
  {
    "id": "js.u04.freq.write",
    "track": "javascript",
    "group": "Hash maps",
    "concept": "frequency counter returning an object",
    "cue": "Loop the string and bump each character's tally.",
    "syntax": "for (let i = 0; i < s.length; i++)",
    "kind": "write",
    "prompt": "Write `charFreq(s)` that returns an object mapping each character of `s` to its count.",
    "starter": "function charFreq(s) {\n  // your code here\n}",
    "solution": "function charFreq(s) {\n  const freq = {};\n  for (let i = 0; i < s.length; i++) freq[s[i]] = (freq[s[i]] || 0) + 1;\n  return freq;\n}",
    "tests": "if (JSON.stringify(charFreq('aabbbc')) !== JSON.stringify({ a: 2, b: 3, c: 1 })) throw new Error('aabbbc');\nif (JSON.stringify(charFreq('')) !== JSON.stringify({})) throw new Error('empty');\nif (charFreq('zzz').z !== 3) throw new Error('zzz');",
    "prereqs": ["js.u04.freq.map", "js.u04.obj.count"]
  },
  {
    "id": "js.u04.build.freq",
    "track": "javascript",
    "group": "Hash maps",
    "concept": "frequency counter on the hand-built map",
    "cue": "Feed each item through the map's own set/get to accumulate counts.",
    "syntax": "map.get(key) || 0",
    "kind": "write",
    "prompt": "Using the finished `MyMap`, write `freqCount(items)` that returns a `MyMap` mapping each item to how many times it appears.",
    "starter": "class MyMap {\n  constructor(size = 8) {\n    this.n = size;\n    this.buckets = Array.from({ length: size }, () => []);\n  }\n  _hash(key) {\n    let h = 0;\n    const s = String(key);\n    for (let i = 0; i < s.length; i++) h = (h * 31 + s.charCodeAt(i)) % this.n;\n    return h;\n  }\n  set(key, val) {\n    const b = this.buckets[this._hash(key)];\n    for (let i = 0; i < b.length; i++) {\n      const pair = b[i];\n      if (pair[0] === key) { pair[1] = val; return this; }\n    }\n    b.push([key, val]);\n    return this;\n  }\n  get(key) {\n    const b = this.buckets[this._hash(key)];\n    for (let i = 0; i < b.length; i++) {\n      const pair = b[i];\n      if (pair[0] === key) return pair[1];\n    }\n    return undefined;\n  }\n  has(key) {\n    const b = this.buckets[this._hash(key)];\n    for (let i = 0; i < b.length; i++) {\n      const pair = b[i];\n      if (pair[0] === key) return true;\n    }\n    return false;\n  }\n}\n\nfunction freqCount(items) {\n  // your code here\n}",
    "solution": "class MyMap {\n  constructor(size = 8) {\n    this.n = size;\n    this.buckets = Array.from({ length: size }, () => []);\n  }\n  _hash(key) {\n    let h = 0;\n    const s = String(key);\n    for (let i = 0; i < s.length; i++) h = (h * 31 + s.charCodeAt(i)) % this.n;\n    return h;\n  }\n  set(key, val) {\n    const b = this.buckets[this._hash(key)];\n    for (let i = 0; i < b.length; i++) {\n      const pair = b[i];\n      if (pair[0] === key) { pair[1] = val; return this; }\n    }\n    b.push([key, val]);\n    return this;\n  }\n  get(key) {\n    const b = this.buckets[this._hash(key)];\n    for (let i = 0; i < b.length; i++) {\n      const pair = b[i];\n      if (pair[0] === key) return pair[1];\n    }\n    return undefined;\n  }\n  has(key) {\n    const b = this.buckets[this._hash(key)];\n    for (let i = 0; i < b.length; i++) {\n      const pair = b[i];\n      if (pair[0] === key) return true;\n    }\n    return false;\n  }\n}\n\nfunction freqCount(items) {\n  const m = new MyMap();\n  for (let i = 0; i < items.length; i++) m.set(items[i], (m.get(items[i]) || 0) + 1);\n  return m;\n}",
    "tests": "const m = freqCount(['a', 'a', 'b', 'a']);\nif (m.get('a') !== 3) throw new Error('a should be 3');\nif (m.get('b') !== 1) throw new Error('b should be 1');\nif (m.get('z') !== undefined) throw new Error('absent -> undefined');",
    "prereqs": ["js.u04.build.has", "js.u04.freq.write"]
  },
  {
    "id": "js.u04.firstunique.predict",
    "track": "javascript",
    "group": "Hash maps",
    "concept": "count pass then ordered scan",
    "cue": "Count first, then scan positions for the earliest character with count one.",
    "syntax": "s[i] index access",
    "kind": "predict",
    "prompt": "What does this evaluate to?",
    "snippet": "const s = 'aabbc';\nconst cnt = {};\nfor (let i = 0; i < s.length; i++) cnt[s[i]] = (cnt[s[i]] || 0) + 1;\nlet res = -1;\nfor (let i = 0; i < s.length; i++) { if (cnt[s[i]] === 1) { res = i; break; } }\nres;",
    "expected": "4",
    "prereqs": ["js.u04.obj.count"]
  },
  {
    "id": "js.u04.firstunique.write",
    "track": "javascript",
    "group": "Hash maps",
    "concept": "first non-repeating character",
    "cue": "Two passes: build counts, then find the first single.",
    "syntax": "for loop with early return",
    "kind": "write",
    "prompt": "Write `firstUniqChar(s)` that returns the index of the first character occurring exactly once, or `-1` if there is none.",
    "starter": "function firstUniqChar(s) {\n  // your code here\n}",
    "solution": "function firstUniqChar(s) {\n  const cnt = {};\n  for (let i = 0; i < s.length; i++) cnt[s[i]] = (cnt[s[i]] || 0) + 1;\n  for (let i = 0; i < s.length; i++) if (cnt[s[i]] === 1) return i;\n  return -1;\n}",
    "tests": "if (firstUniqChar('aabbc') !== 4) throw new Error('aabbc');\nif (firstUniqChar('leetcode') !== 0) throw new Error('leetcode');\nif (firstUniqChar('loveleetcode') !== 2) throw new Error('loveleetcode');\nif (firstUniqChar('aabb') !== -1) throw new Error('all repeat');\nif (firstUniqChar('') !== -1) throw new Error('empty');",
    "mapsTo": "LC 387 · First Unique Character in a String",
    "prereqs": ["js.u04.firstunique.predict"]
  },
  {
    "id": "js.u04.twosum.seen",
    "track": "javascript",
    "group": "Hash maps",
    "concept": "complement lookup in one pass",
    "cue": "Track seen values and look for each element's complement.",
    "syntax": "target - nums[i]",
    "kind": "predict",
    "prompt": "What does this evaluate to?",
    "snippet": "const nums = [2, 7, 11, 15];\nconst target = 9;\nconst seen = new Map();\nlet ans = null;\nfor (let i = 0; i < nums.length; i++) {\n  const c = target - nums[i];\n  if (seen.has(c)) { ans = [seen.get(c), i]; break; }\n  seen.set(nums[i], i);\n}\nans;",
    "expected": "[0, 1]",
    "prereqs": ["js.u04.map.basics"]
  },
  {
    "id": "js.u04.twosum.write",
    "track": "javascript",
    "group": "Hash maps",
    "concept": "two-sum via complement map",
    "cue": "Check for the complement in the seen map before recording the current value.",
    "syntax": "map.has(complement)",
    "kind": "write",
    "prompt": "Write `twoSum(nums, target)` that returns `[i, j]` of two distinct indices whose values sum to `target`, or `null` if none exist. Make a single pass and check the complement before storing the current value.",
    "starter": "function twoSum(nums, target) {\n  // your code here\n}",
    "solution": "function twoSum(nums, target) {\n  const seen = new Map();\n  for (let i = 0; i < nums.length; i++) {\n    const c = target - nums[i];\n    if (seen.has(c)) return [seen.get(c), i];\n    seen.set(nums[i], i);\n  }\n  return null;\n}",
    "tests": "if (JSON.stringify(twoSum([2, 7, 11, 15], 9)) !== JSON.stringify([0, 1])) throw new Error('basic');\nif (JSON.stringify(twoSum([3, 3], 6)) !== JSON.stringify([0, 1])) throw new Error('duplicates');\nif (JSON.stringify(twoSum([3, 2, 4], 6)) !== JSON.stringify([1, 2])) throw new Error('not first two');\nif (twoSum([1, 2, 3], 100) !== null) throw new Error('no pair -> null');",
    "mapsTo": "LC 1 · Two Sum",
    "note": "Storing seen values as you go turns the O(n^2) nested scan into one pass: each complement lookup is O(1).",
    "prereqs": ["js.u04.twosum.seen"]
  },
  {
    "id": "js.u04.groupby.buckets",
    "track": "javascript",
    "group": "Hash maps",
    "concept": "group-by into value lists",
    "cue": "Bucket each number by its parity, appending into the matching list.",
    "syntax": "[...map.values()]",
    "kind": "predict",
    "prompt": "What does this evaluate to?",
    "snippet": "const nums = [1, 2, 3, 4, 5];\nconst m = new Map();\nfor (let i = 0; i < nums.length; i++) {\n  const x = nums[i];\n  const k = x % 2;\n  if (!m.has(k)) m.set(k, []);\n  m.get(k).push(x);\n}\n[...m.values()];",
    "expected": "[[1, 3, 5], [2, 4]]",
    "prereqs": ["js.u04.map.basics"]
  },
  {
    "id": "js.u04.groupby.write",
    "track": "javascript",
    "group": "Hash maps",
    "concept": "group anagrams by sorted-char key",
    "cue": "Key each word by its sorted letters and append into that group.",
    "syntax": "arr.sort() then join('')",
    "kind": "write",
    "prompt": "Write `groupAnagrams(words)` that returns an array of groups, each group holding the words that are anagrams of each other. Key each group by its sorted characters. Preserve the order in which each group's key was first seen, and within a group the input order.",
    "starter": "function groupAnagrams(words) {\n  // your code here\n}",
    "solution": "function groupAnagrams(words) {\n  const m = new Map();\n  for (let i = 0; i < words.length; i++) {\n    const w = words[i];\n    const key = [...w].sort().join('');\n    if (!m.has(key)) m.set(key, []);\n    m.get(key).push(w);\n  }\n  return [...m.values()];\n}",
    "tests": "const got = groupAnagrams(['eat', 'tea', 'tan', 'ate', 'nat', 'bat']);\nconst want = [['eat', 'tea', 'ate'], ['tan', 'nat'], ['bat']];\nif (JSON.stringify(got) !== JSON.stringify(want)) throw new Error(JSON.stringify(got));\nif (JSON.stringify(groupAnagrams([])) !== JSON.stringify([])) throw new Error('empty');",
    "mapsTo": "LC 49 · Group Anagrams",
    "prereqs": ["js.u04.groupby.buckets"]
  },
  {
    "id": "js.u04.valindex.map",
    "track": "javascript",
    "group": "Hash maps",
    "concept": "value to index, last write wins",
    "cue": "Trace which index survives when a value repeats.",
    "syntax": "for (let i = 0; i < arr.length; i++)",
    "kind": "predict",
    "prompt": "What does this evaluate to?",
    "snippet": "const nums = [5, 3, 5, 7];\nconst pos = new Map();\nfor (let i = 0; i < nums.length; i++) pos.set(nums[i], i);\npos.get(5);",
    "expected": "2",
    "prereqs": ["js.u04.map.basics"]
  },
  {
    "id": "js.u04.valindex.write",
    "track": "javascript",
    "group": "Hash maps",
    "concept": "value to first index",
    "cue": "Store an index only the first time you meet each value.",
    "syntax": "if (!map.has(v))",
    "kind": "write",
    "prompt": "Write `buildIndex(nums)` that returns a `Map` from each value to the index of its FIRST occurrence.",
    "starter": "function buildIndex(nums) {\n  // your code here\n}",
    "solution": "function buildIndex(nums) {\n  const pos = new Map();\n  for (let i = 0; i < nums.length; i++) if (!pos.has(nums[i])) pos.set(nums[i], i);\n  return pos;\n}",
    "tests": "const p = buildIndex([5, 3, 5, 7]);\nif (p.get(5) !== 0) throw new Error('first 5 is index 0');\nif (p.get(3) !== 1) throw new Error('3');\nif (p.get(7) !== 3) throw new Error('7');\nif (p.get(9) !== undefined) throw new Error('absent');\nif (p.size !== 3) throw new Error('distinct values only');",
    "prereqs": ["js.u04.valindex.map"]
  },
  {
    "id": "js.u04.seen.dedup",
    "track": "javascript",
    "group": "Hash maps",
    "concept": "contains-duplicate via Set size",
    "cue": "Compare the deduped size against the original length.",
    "syntax": "new Set(arr).size",
    "kind": "predict",
    "prompt": "What does this evaluate to?",
    "snippet": "const arr = [1, 2, 2, 3];\nnew Set(arr).size !== arr.length;",
    "expected": "true",
    "mapsTo": "LC 217 · Contains Duplicate",
    "prereqs": []
  },
  {
    "id": "js.u04.seen.set-value",
    "track": "javascript",
    "group": "Hash maps",
    "concept": "Set keeps first occurrences",
    "cue": "A Set drops later repeats; read back what survives.",
    "syntax": "new Set(iterable)",
    "kind": "predict",
    "prompt": "What does this evaluate to?",
    "snippet": "const arr = [3, 1, 2, 3, 1];\nnew Set(arr);",
    "expected": "Set(3) {3, 1, 2}",
    "prereqs": ["js.u04.seen.dedup"]
  },
  {
    "id": "js.u04.seen.dedup.write",
    "track": "javascript",
    "group": "Hash maps",
    "concept": "order-preserving dedup with a seen set",
    "cue": "Track seen values and emit each the first time only.",
    "syntax": "set.add(x)",
    "kind": "write",
    "prompt": "Write `dedup(arr)` that returns a new array with duplicates removed, keeping the first occurrence of each value in its original order.",
    "starter": "function dedup(arr) {\n  // your code here\n}",
    "solution": "function dedup(arr) {\n  const seen = new Set();\n  const out = [];\n  for (let i = 0; i < arr.length; i++) if (!seen.has(arr[i])) { seen.add(arr[i]); out.push(arr[i]); }\n  return out;\n}",
    "tests": "if (JSON.stringify(dedup([3, 1, 2, 3, 1])) !== JSON.stringify([3, 1, 2])) throw new Error('order preserved');\nif (JSON.stringify(dedup([])) !== JSON.stringify([])) throw new Error('empty');\nif (JSON.stringify(dedup([7, 7, 7])) !== JSON.stringify([7])) throw new Error('all same');",
    "prereqs": ["js.u04.seen.set-value"]
  },
  {
    "id": "js.u04.topk.predict",
    "track": "javascript",
    "group": "Hash maps",
    "concept": "sort count-map entries by value",
    "cue": "Count first, then order the entries by their count before taking a slice.",
    "syntax": "[...map.entries()].sort((a, b) => b[1] - a[1])",
    "kind": "predict",
    "prompt": "What does this evaluate to?",
    "snippet": "const nums = [1, 1, 1, 2, 2, 3];\nconst m = new Map();\nfor (let i = 0; i < nums.length; i++) m.set(nums[i], (m.get(nums[i]) || 0) + 1);\n[...m.entries()].sort((a, b) => b[1] - a[1]).slice(0, 2).map(e => e[0]);",
    "expected": "[1, 2]",
    "prereqs": ["js.u04.freq.map"]
  },
  {
    "id": "js.u04.topk.write",
    "track": "javascript",
    "group": "Hash maps",
    "concept": "top k frequent elements",
    "cue": "Build a frequency map, then order its entries by count and take the leaders.",
    "syntax": "arr.slice(0, k).map(...)",
    "kind": "write",
    "prompt": "Write `topKFrequent(nums, k)` that returns the `k` values that appear most often, most frequent first.",
    "starter": "function topKFrequent(nums, k) {\n  // your code here\n}",
    "solution": "function topKFrequent(nums, k) {\n  const m = new Map();\n  for (let i = 0; i < nums.length; i++) m.set(nums[i], (m.get(nums[i]) || 0) + 1);\n  return [...m.entries()].sort((a, b) => b[1] - a[1]).slice(0, k).map(e => e[0]);\n}",
    "tests": "if (JSON.stringify(topKFrequent([1, 1, 1, 2, 2, 3], 2)) !== JSON.stringify([1, 2])) throw new Error('basic');\nif (JSON.stringify(topKFrequent([4, 4, 4, 5, 5, 6, 6, 6, 6], 1)) !== JSON.stringify([6])) throw new Error('single leader');\nif (JSON.stringify(topKFrequent([7], 1)) !== JSON.stringify([7])) throw new Error('one element');",
    "mapsTo": "LC 347 · Top K Frequent Elements",
    "prereqs": ["js.u04.topk.predict"]
  },
  {
    "id": "js.u04.subarraysum.predict",
    "track": "javascript",
    "group": "Hash maps",
    "concept": "prefix-sum counts via a seen map",
    "cue": "Carry a running sum; a subarray ends here for every earlier prefix equal to sum minus k.",
    "syntax": "seen.get(sum - k)",
    "kind": "predict",
    "prompt": "What does this evaluate to?",
    "snippet": "const nums = [1, 1, 1];\nconst k = 2;\nconst seen = new Map([[0, 1]]);\nlet sum = 0, count = 0;\nfor (let i = 0; i < nums.length; i++) {\n  sum += nums[i];\n  if (seen.has(sum - k)) count += seen.get(sum - k);\n  seen.set(sum, (seen.get(sum) || 0) + 1);\n}\ncount;",
    "expected": "2",
    "note": "seen maps each running prefix sum to how many times it occurred; seeding {0: 1} counts subarrays that start at index 0.",
    "prereqs": ["js.u04.freq.map"]
  },
  {
    "id": "js.u04.subarraysum.write",
    "track": "javascript",
    "group": "Hash maps",
    "concept": "count subarrays summing to k",
    "cue": "Keep a map of prefix-sum frequencies and add in every match as you scan.",
    "syntax": "map of prefix-sum -> count",
    "kind": "write",
    "prompt": "Write `subarraySum(nums, k)` that returns how many contiguous subarrays sum to `k`. Values may be negative.",
    "starter": "function subarraySum(nums, k) {\n  // your code here\n}",
    "solution": "function subarraySum(nums, k) {\n  const seen = new Map([[0, 1]]);\n  let sum = 0, count = 0;\n  for (let i = 0; i < nums.length; i++) {\n    sum += nums[i];\n    if (seen.has(sum - k)) count += seen.get(sum - k);\n    seen.set(sum, (seen.get(sum) || 0) + 1);\n  }\n  return count;\n}",
    "tests": "if (subarraySum([1, 1, 1], 2) !== 2) throw new Error('basic');\nif (subarraySum([1, 2, 3], 3) !== 2) throw new Error('two ways');\nif (subarraySum([1, -1, 0], 0) !== 3) throw new Error('negatives and zeros');\nif (subarraySum([1, 2, 3], 7) !== 0) throw new Error('none');",
    "mapsTo": "LC 560 · Subarray Sum Equals K",
    "prereqs": ["js.u04.subarraysum.predict"]
  },
  {
    "id": "js.u04.longestconsec.predict",
    "track": "javascript",
    "group": "Hash maps",
    "concept": "count runs only from sequence starts",
    "cue": "Put everything in a set; only begin counting a run at a value whose predecessor is absent.",
    "syntax": "set.has(x - 1)",
    "kind": "predict",
    "prompt": "What does this evaluate to?",
    "snippet": "const nums = [100, 4, 200, 1, 3, 2];\nconst s = new Set(nums);\nlet best = 0;\nfor (let i = 0; i < nums.length; i++) {\n  const x = nums[i];\n  if (!s.has(x - 1)) {\n    let len = 1;\n    while (s.has(x + len)) len++;\n    if (len > best) best = len;\n  }\n}\nbest;",
    "expected": "4",
    "note": "Skipping any x whose x-1 is present means each run is walked once from its start, so the whole scan is O(n), not O(n log n).",
    "prereqs": ["js.u04.seen.set-value"]
  },
  {
    "id": "js.u04.longestconsec.write",
    "track": "javascript",
    "group": "Hash maps",
    "concept": "longest consecutive sequence",
    "cue": "Load a set, then from each run-start count upward while the next value is present.",
    "syntax": "new Set(nums)",
    "kind": "write",
    "prompt": "Write `longestConsecutive(nums)` that returns the length of the longest run of consecutive integers in `nums`, in O(n) time. The values need not be adjacent in the array.",
    "starter": "function longestConsecutive(nums) {\n  // your code here\n}",
    "solution": "function longestConsecutive(nums) {\n  const s = new Set(nums);\n  let best = 0;\n  for (let i = 0; i < nums.length; i++) {\n    const x = nums[i];\n    if (!s.has(x - 1)) {\n      let len = 1;\n      while (s.has(x + len)) len++;\n      if (len > best) best = len;\n    }\n  }\n  return best;\n}",
    "tests": "if (longestConsecutive([100, 4, 200, 1, 3, 2]) !== 4) throw new Error('basic');\nif (longestConsecutive([]) !== 0) throw new Error('empty');\nif (longestConsecutive([1, 2, 0, 1]) !== 3) throw new Error('duplicates');\nif (longestConsecutive([0, 3, 7, 2, 5, 8, 4, 6, 0, 1]) !== 9) throw new Error('long run');",
    "mapsTo": "LC 128 · Longest Consecutive Sequence",
    "prereqs": ["js.u04.longestconsec.predict"]
  }
]
